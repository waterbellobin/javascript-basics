const COORDS = 'coords';

function loadCoords(){
    const loadedCoords = localStorage.getItem
    if (loadCoords === null){
        askForCoords();
    } else {

    }
}

function init(){

}

init();